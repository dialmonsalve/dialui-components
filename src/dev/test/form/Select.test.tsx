import { render, screen } from '@testing-library/react';

import { beforeEach } from 'vitest';

describe('Select', () => {
	test('should sum one plus one Example component', () => {
		const textElement = 1 + 1;

		expect(textElement).toBe(2);
	});
});